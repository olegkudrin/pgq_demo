[welcome_app]
job_name = welcome_consumer

src_db = host=localhost dbname=dms user=dms password=dms

pgq_queue_name = notifications
pgq_consumer_id = %(job_name)s

logfile = log/%(job_name)s.log
pidfile = pid/%(job_name)s.pid
